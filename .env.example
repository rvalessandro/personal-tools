# Telegram Bot
TELEGRAM_BOT_TOKEN=
ALLOWED_USER_IDS=

# Workspace that Claude can access
WORKSPACE_PATH=

# Claude config (contains OAuth tokens + MCP configs)
# Must have authenticated `claude` CLI first
CLAUDE_CONFIG_PATH=~/.claude

# Calendar Sync
CALENDARSYNC_ENCRYPTION_KEY=
